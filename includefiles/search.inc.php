<?php
//arsort(array);
/*$JSON = {
	"name" : "mialn rai",
}
$Object = array("Milan Rai"=> 5,"Sam"=>2,"Deku"=>3);

print($Object["Milan Rai"]);
 $jobj = json_encode($Object);
echo $jobj;
$name = "Sam";
$string = json_decode($jobj, true);
echo $string[$name];

    /*array_push($someArray, [
      'name'   => $row['name'],
      'gender' => $row['gender']
    ]);*/
$false = 0;
var_dump((bool)$false);
?>
<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<div id="bod">
		
	</div>
</body>
</html>
<script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
<script>
	/*var self = document.getElementById('bod');
	var peopleHTML ="";
	var arrayObject = [
		{"name" : "Milan", "age" : 20},
		{"name" : "Cm rai", "age" : 20},
		{"name" : "sam", "age" : 20},
		{"name" : "aida", "age" : 20}
	];
	console.log(arrayObject);
	var JSONObject = (arrayObject);
	console.log(JSONObject.length);
	for (var i = 0; i < JSONObject.length; i++){
		console.log(JSONObject[i]["name"]);
		console.log(JSONObject[i]["age"]);
		 peopleHTML += "<hr><br>" + JSONObject[i]["name"] + "<br>" + JSONObject[i]["age"] +"<br>";
	}
	self.innerHTML = peopleHTML;

$("#gender").on("change", function() {
  $.ajax({
    type: "POST",
    data: {
      "gender": $("#gender").val()
    },
    url: "response.php",
    dataType: "json",
    success: function(JSONObject) {
      var peopleHTML = "";

      // Loop through Object and create peopleHTML
      for (var key in JSONObject) {
        if (JSONObject.hasOwnProperty(key)) {
          peopleHTML += "<tr>";
            peopleHTML += "<td>" + JSONObject[key]["name"] + "</td>";
            peopleHTML += "<td>" + JSONObject[key]["gender"] + "</td>";
          peopleHTML += "</tr>";
        }
      }

      // Replace table’s tbody html with peopleHTML
      $("#people tbody").html(peopleHTML);
    }
  });
});*/
</script>



